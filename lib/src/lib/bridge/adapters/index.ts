/**
 * Adapter catalog for the shared bridge core.
 *
 * Telegram stays built into the shared runtime.
 * Additional adapters can be registered by consuming host repos at startup.
 */

import './telegram-adapter.js';
